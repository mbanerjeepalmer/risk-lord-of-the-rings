[project]
name = "risk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.0.0",
    "rich>=13.0.0",
]

[project.scripts]
risk-battle = "battle_probability:main"
